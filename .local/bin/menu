#!/bin/sh
if command -v rofi>/dev/null; then
  alias menu='rofi -dmenu'
elif command -v dmenu>/dev/null; then
  alias menu='dmenu -i -matching fuzzy'
else
  notify-send -t 3000 -u critical "dmenu-pdfs" "'dmenu and rofi aren't installed"
  exit
fi

menu
